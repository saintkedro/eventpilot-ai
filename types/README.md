# Shared types (`types/`)

Global TypeScript types and generated Supabase database types.

- `database.ts` — placeholder for `supabase gen types typescript` output
- Re-export shared app types from `index.ts` as they are introduced

Feature-specific types stay in `features/<name>/types/`.
