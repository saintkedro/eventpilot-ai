# Tests (`tests/`)

| Folder | Scope |
|--------|--------|
| `unit/` | Pure functions, small modules |
| `integration/` | Database, API routes with test containers or Supabase local |
| `e2e/` | Playwright (or similar) against running app |

Add test runner config at repo root when introducing a framework.
